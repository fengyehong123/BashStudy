; 执行自动登录脚本
include '00-自動ログイン.ttl'

; 将脚本终止1秒钟，目的是等到登录界面完全显示之后
; 再继续执行下面的脚本，可以保证弹出框显示在登录界面的上一层
pause 1

; 定义包含环境变量路径的字符串
str1 = "%WINDIR%\notepad.exe"
str2 = "%JAVA_HOME%\bin"

; 将环境变量的路径转换为实际的路径
expandenv str1
expandenv str2

; 将字符串打印在控制台
sendln 'echo 字符串1的内容为 → ' str1
wait username

sendln 'echo 字符串2的内容为 → ' str2
wait username